{
  "name": "ai-testcase-generator",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web \"cd backend && uvicorn app.main:app --reload\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
